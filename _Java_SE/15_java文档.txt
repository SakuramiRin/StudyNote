Applicaton Progarm Interface
/**
自动chm
文档@param 参数
@return
 */
(用IDE自动去生成或者去自查)

javadoc -d(目录) myhelp -author -version ArrayTool.java  生成的目录

注意：必须要public暴露出该类！！！---->即，一个类中默认的空参构造函数权限和所属类是一致的

---------------------
jdk的升级主要围绕的是：安全性，效率性，书写简易度
